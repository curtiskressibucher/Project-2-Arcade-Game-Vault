<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Arcade Game Vault</title>
        <link rel="stylesheet" href="style/style.css" />
        <script src="js/script.js"></script>
    </head>
    <body class="page-background">
        <nav>
            <div class="links">
                <a href="/home">Home</a>
                <a href="/about">About</a>
                 <% if(user) {%>
                <a href="/games">Vault</a>
                <% } %>
                <% if(user) { %>
                <a href="/logout"
                    >Log Out<img
                        class="user-avatar"
                        src="<%= user.avatar %>"
                        alt="User avatar"
                /></a>
                <% } else { %>
                <a href="/login">Login</a>
                <% } %>
            </div>
            <div class="burger-menu">&#9776;</div>
        </nav>
        <div class="dropdown-menu">
               <a href="/home">Home</a>
                <a href="/about">About</a>
                 <% if(user) {%>
                <a href="/games">Vault</a>
                <% } %>
                <% if(user) { %>
                <a href="/logout"
                    >Log Out<img
                        class="user-avatar"
                        src="<%= user.avatar %>"
                        alt="User avatar"
                /></a>
                <% } else { %>
                <a href="/login">Login</a>
                <% } %>
        </div>
        <header>
            <div class="Logo">
                <img src="/images/Arcade-gaming-vault-logo.png" alt="Logo" />
            </div>
        </header>

        <% if(user) { %>
        <h2 class="login-button">Welcome to your vault</h2>
        <h1><%= user.name %></h1>
        <% } else { %>
        <div class="login-button">
            <a href="/login">LOGIN</a>
        </div>
        <% } %>

        <div class="web-feat">

            <a href="<% if(user) { %>/games<% } else { %>/login<% } %>" id="game-vault">
            <h2 class="web-feat-button">GAME VAULT</h2>
            </a>

            <a href="/reviews" id="Reviews">
            <h2 class="web-feat-button">REVIEWS</h2>
            </a>

            <a href="/tips-and-tricks" id="Tips-Tricks">
            <h2 class="web-feat-button">TIPS AND TRICKS</h2>
            </a>

        </div>

        <footer>&copy; 2023 Arcade Game Vault. All rights reserved.</footer>
    </body>
</html>
